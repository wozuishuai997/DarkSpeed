# DarkSpeed 开发、编译部署与整改说明

整理日期：2026-09-12  
文档基线：`1.0-9`，提交 `d0aff7b`  
适用分支：`codex/clock-top-positions`

## 1. 项目及当前交付状态

DarkSpeed 是 iPhone 状态栏悬浮显示项目。本次在原有网速、FPS 功能上，增加时间显示、顶部位置、字体与背景设置、水平偏移、可配置刷新间隔，以及透明文字跟随系统状态栏颜色等功能。`1.0-9` 针对 **SpringBoard 在 `0x401` 地址崩溃** 做了专项修复，并在远程调用异常时改为跳过刷新（fail-closed）。

| 项目 | 当前信息 |
|---|---|
| 上游仓库 | <https://github.com/huami1314/DarkSpeed> |
| 开发仓库 | <https://github.com/wozuishuai997/DarkSpeed> |
| 本地目录 | `C:\Users\Public\Git\DarkHumi` |
| 实际使用设备 | iPhone 15 Pro，iOS 18.6.2，系统构建 22G100 |
| 应用标识 | `com.huami.darkspeed` |
| 当前版本号 / 构建号 | `1.0` / `9` |
| 当前安装包 | [packages/DarkSpeed_1.0-9.ipa](packages/DarkSpeed_1.0-9.ipa) |
| 安装方式 | 未签名 IPA，经用户使用 LCSign 签名后安装 |
| 验证状态 | 云端编译、归档与安装包校验通过；`0x401` 崩溃修复**尚未经过实机回归** |

**`1.0-9` 是崩溃修复的测试包，不能仅凭编译成功断言崩溃已消失。** 该修复依据五份崩溃日志的确定性证据定位并加固了相关代码路径，但结论仍需在 iOS 18.6.2 上实测确认。

本次核对时，原 `README.md` 和 `README_ZH.md` 已同步为 `1.0-9`。当前版本应以工程配置、安装包内 `Info.plist` 和本说明的基线为准。


## 2. 代码结构与维护入口

| 文件 | 作用 |
|---|---|
| `darksword/DSBridge.mm` | 当前 IPA 的主要悬浮窗渲染路径；读取设置、计算内容和位置、执行定时刷新、应用字体与颜色 |
| `sources/RootViewController.mm` | 应用主界面、设置读写、保存后发送刷新通知 |
| `sources/TSSettingsController.swift` | 设置交互；字号选择、偏移输入、刷新间隔选择及选项启用状态 |
| `sources/TSSettingsIndex.swift` | 设置项、标题、键名及当前值文案 |
| `supports/hudapp-bridging-header.h` | 共享设置键、显示模式枚举、描边宽度与刷新间隔取值 |
| `sources/HUDRootViewController.mm` | 原有 HUD 控制器路径，不能代替当前 DSBridge 路径的实机验证 |
| `sources/HUDBackdropLabel.h/.mm` | 原有文字控件，包含描边内边距及旧背景反色实现 |
| `Resources/*.lproj/Localizable.strings` | 应用中文、英文、西班牙文文案 |
| `Resources/Settings.bundle/Root.plist` | 系统设置中的高级字号、偏移等配置 |
| `DarkSpeed.xcodeproj/project.pbxproj` | Xcode 工程、版本号、构建号及各目标配置 |
| `build-darkspeed.sh` | 应用构建、归档和 IPA 打包入口 |
| `get-version.sh` | 从工程配置读取版本号和构建号 |
| `.github/workflows/build-test-ipa.yml` | 本次使用的测试构建流程 |
| `.github/workflows/build-release.yml` | 原有公开 Release 发布流程，与测试流程不同 |

当前渲染器与原有 HUD 控制器是不同路径。修改其中一个文件，不代表另一条路径自动获得相同行为。`1.0-6` 的系统状态栏颜色跟随实现位于当前 DSBridge 渲染器。

设置主要保存在 HUD 配置文件中；历史高级字号、偏移保存在标准偏好设置中。当前渲染器会合并这两类设置。字号入口沿用原有高级字号存储，避免界面与渲染器读取不同值。

## 3. 功能规格与使用说明

### 3.1 显示模式

依次切换：网速 → FPS → 时间 → 时间（秒）。

- 时间：显示小时、分钟。
- 时间（秒）：显示小时、分钟、秒。
- 使用系统日期格式，跟随手机的 12/24 小时制、地区和时区设置。
- 保留旧模式的枚举值：网速为 0、FPS 为 1，时间和时间（秒）追加为 2、3，避免旧设置错位。
- 选择较长刷新间隔后，时间（秒）会跳着更新，不会在间隔期间另行每秒更新。

### 3.2 位置与水平偏移

- 支持左、中、右位置及对应顶部位置。
- 重复点击当前选中的位置按钮，可切换普通位置与顶部位置。
- 顶部两侧位置保留屏幕圆角避让，因此大字号时可能感觉文字向内收缩。
- 新增“水平偏移”：范围 −100～+100，负数向左，正数向右，0 恢复新增偏移前的位置。
- 偏移单位为界面点，不是截图像素；统一作用于左、中、右及顶部位置。
- 新增偏移在原有位置计算后应用，不修改原高级垂直偏移配置；如果之前已有高级水平偏移，两者会叠加。
- 例如右侧顶部文字偏内，可先尝试 `+10`，再按实际显示调整。

### 3.3 字号与加粗

- 点击“字号”，直接选择 8～24 号，共 17 个整数档位。
- 列表标记当前值，设置入口显示实际字号。
- 未主动选择新字号时，保留已有设置；原标准/较大设置分别对应 9/10 号。
- 加粗为独立开关，当前使用系统最粗字重 `UIFontWeightBlack`。
- 描边宽度按字号百分比计算：普通文字为 −2，加粗为 −3。负值表示同时绘制文字填充与描边。
- 文字测量使用对应字重，并为描边预留空间。

### 3.4 背景、文字和描边颜色

| 背景模式 | 当前行为 |
|---|---|
| 原样 | 保留原有背景及手动黑白外观设置 |
| 透明（跟随系统） | 文字跟随系统右侧状态栏的黑白样式，描边取相反颜色 |

透明模式只跟随状态栏的黑白显示，不模拟低电量黄色、充电绿色等电池状态色。透明模式下，手动外观选项暂时禁用；切回原样后恢复。

当前通过内置屏幕右侧状态栏的有效样式获取颜色，优先考虑系统提供的前景灰度，再判断黑白样式。不对屏幕截图取色，也不按 DarkSpeed 自身的界面主题推断其他应用的状态栏颜色。

读取入口依据 iOS 18.2 的公开反编译资料核对；iOS 18.6.2 上仍需实机验证。不支持或读取不到有效颜色时，保留原颜色。颜色检查沿用配置的刷新周期，没有新增独立每秒轮询。

### 3.5 实际刷新间隔

档位：**1、2、3、5、10、15、30、60 秒**，默认 1 秒。

实现直接调整周期定时器，不是保持每秒采样后仅跳过部分文字绘制。网速按实际经过时间计算，FPS 也按实际采样时间计算，避免把多秒累计值显示成单秒值。

需要区分周期更新与事件更新：

- 日常采样、文字、位置及状态栏颜色检查使用所选周期。
- 修改设置、锁屏或解锁仍可能触发一次即时更新。
- iOS 的线程调度、后台状态可能导致定时器延迟；所选间隔不是实时系统的严格时间保证。
- 调低频率是否能缓解崩溃尚未验证，不属于已经确认的崩溃修复。

### 3.6 横竖屏行为边界

`1.0-2` 已将方向来源改为活动界面方向，以处理视频实际横屏后桌面方向仍不变的问题。当前方向检查仍位于周期刷新流程内，因此较长刷新间隔也会延迟方向检测及横屏隐藏。

用户后来提出“切换横屏和竖屏时也要刷新”。讨论过独立每秒检测方向、变化时额外刷新，但该方案增加系统访问次数，**尚未获得确认，也未实现**。不要把当前版本描述为横竖屏事件即时刷新。

## 4. 历次开发与整改记录

| 版本 | 提交 | 主要内容 |
|---|---|---|
| 1.0-1 | `044d3f4` | 增加时间模式、左右顶部位置及测试构建工作流 |
| 1.0-2 | `9c4d561` | 增加时间（秒）；调整横屏方向来源及相关显示状态处理 |
| 1.0-3 | `52b7344` | 增加文字加粗、透明背景、反色描边和描边空间 |
| 1.0-4 | `72bc5cc` | 增加 8～24 号选择；提高字重；修改设置刷新判断；细化描边和文字属性 |
| 1.0-5 | `e1a6892` | 增加水平偏移、实际刷新间隔；按实际采样时长计算速率 |
| 1.0-6 | `e873cd4` | 透明文字跟随系统右侧状态栏黑白样式，描边反色 |
| 1.0-7 | `421cb77` | 修复 SpringBoard 在 `0x401` 标记地址崩溃；远程调用失败时改为跳过刷新 |

### 4.1 SpringBoard 崩溃的分析结论与修复（1.0-7 → 1.0-9）

本问题共有**两种**崩溃形态，第二种才是「开启悬浮窗即崩」的直接原因，由 1.0-7 的实机日志 `SpringBoard-2026-09-12-083831.ips` 定位。

#### 形态一：`0x401` 上的 SIGBUS（`1.0-7` 起加固）

五份崩溃日志（`SpringBoard-2026-09-11-204251 / 212213 / 220219 / 225431 / 232558.ips`）是**同一个故障**：

```text
exception   : EXC_BAD_ACCESS / SIGBUS
subtype     : EXC_ARM_DA_ALIGN at 0x0000000000000401
vmRegionInfo: 0x401 is not in any region.
esr         : 0x8a000000   （指令取指翻译错误）
```

`0x401` 不是随机地址，而是源码中的 `FAKE_LR_TROJAN`——远程调用协议用来当“返回陷阱”的哨兵值（另外还有 `0x101/0x201/0x301`）。崩溃线程的寄存器结构可以直接判定它是**刚开始执行第一条指令的全新 pthread**：

| 观察 | 数值 | 含义 |
|---|---|---|
| `sp` | `0x16b32efd0` | 位于栈页边界下方 16 字节 |
| `fp` | `sp + 16` | 函数序言尚未执行 |
| `x20` / `x21` | `0` / `0` | 线程尚无任何业务状态 |
| 帧 1 | `libsystem_pthread + 2744` = `thread_start` | 该线程的启动例程入口 |

即：**一个原始哨兵值被写进了活动线程的可执行地址**，线程第一次取指即在未映射的 `0x401` 上中止，而该线程当时并不属于任何异常端口，于是整个 SpringBoard 被终止（`termination.byProc = "exc handler"`）。

加固内容：

1. **PAC 探测线程的异常端口被过早销毁**（`pac.m`）。每次 `remotepac()` 都新建一个端口，并在 100 ms 等待返回后立刻 `mach_port_destruct`。若该线程的中止在窗口之后才投递，线程就再无异常端口可投递，中止直接杀死目标进程。现在探测端口放入**永不释放的端口环**，等待改为 3 秒 × 3 轮，失败一律 `return 0`。
2. **原始哨兵值被当成新线程的启动例程**（`RemoteCall.m`）。`remotepac()` 的结果直接交给 `pthread_create_suspended_np`；内核无法认证启动例程时会把它降级为裸值，线程随即在 `0x101/0x201/0x301/0x401` 上中止。现在低于页大小的启动例程会被**拒绝并要求重新初始化**。

#### 形态二：主线程上的 PAC_EXCEPTION（`1.0-9` 修复，开启悬浮窗即崩的直接原因）

`SpringBoard-2026-09-12-083831.ips`（1.0-7 实机）：

```text
exception      : EXC_BAD_ACCESS / SIGKILL
subtype        : EXC_ARM_DA_ALIGN at 0x2000000000000101 -> 0x0000000000000101
                 (possible pointer authentication failure)
termination    : namespace = PAC_EXCEPTION, code = 257
faultingThread : 0   ← SpringBoard 主线程，栈在 __CFRunLoopRun 里
pc             : 0x101               = FAKE_PC_TROJAN_CREATOR
lr             : 0x2000000000000201  = FAKE_LR_TROJAN_CREATOR
```

`0x101`/`0x201` 正是 RemoteCall 初始化时**第一次回复**所用的停车标记（`parkState: pc=firstThreadParkTrap lr=_firstThreadReturnTrap`）。根因链条：

1. `remotepac()` 无法为该线程产生可用签名，`signState()` 旧逻辑**回退写入裸指针** `0x101`。
2. 调用方仍然照常 `statereply()` 把状态交回内核。
3. 内核在异常返回时直接恢复该线程，**拒绝未正确签名的标记 PC**，以 `PAC_EXCEPTION` 杀死整个 SpringBoard —— 而这条线程是主线程，所以表现为“开启悬浮窗就崩”。

修复：

- `signState()` 改为返回 `BOOL`，**不再回退裸 PC**，而是明确报告该 PC 无法签名。
- 所有可能携带标记 PC 的回复点，在签名失败时**一律拒绝回复**，把线程留在内核异常里（这是无害的挂起，不是崩溃）：两条初始化停车路径、`doRemoteCallInternalTimeout`、`doRemoteCallWithPendingException`、函数入口停车、临时陷阱恢复、`restoreTrojanThreadWithState`、主线程恢复。
- 结果：探测失败只导致**启用失败**，不再导致进程死亡。
- `pac.m` 撤掉了对异常动作的额外内核回读校验。该校验一旦出现假阴性就会否掉每一次签名，直接导致初始化失败（实机表现为「SpringBoard 连接失败：RemoteCall init failed」），本身也是裸标记回退的来源。`thread_set_exception_ports` 返回成功即为权威依据，且端口环永不释放，`0x401` 中止始终有服务者。

**fail-closed 行为**：`RemoteCall` 增加 `isHealthy` 健康标志，遇到意外陷阱、线程状态恢复失败、函数入口故障等情况即锁存为不可用。

`1.0-9` 进一步把这道闸门前移到**最底层**并覆盖**创建阶段**：

- 所有底层远程调用辅助函数（`ds_remote_sel`、`ds_remote_class`、`ds_remote_create_string`、`ds_perform_on_springboard_main`）统一经过 `ds_remote_process_usable()` 判断，新增调用点无法绕过闸门。
- 连接在 `RemoteCall` 校验通过后、`ds_create_springboard_hud()` **之前**就注册为活动连接。1.0-7 只在创建成功后才注册，导致创建阶段（数百次远程调用、每次都产生 PAC 探测）完全不受闸门保护——连接若在创建中途变坏，仍会被继续驱动，而每一次多余调用都是把线程送向 `0x401` 的机会。

一旦闸门关闭，界面显示「为保护 SpringBoard，已停止 HUD 刷新：…」并停止一切远程调用。**刷新可能停止，但 SpringBoard 不会被这个路径打崩。** 看到该文案属于预期保护行为，不是新故障；此时应提供 `DSBridge.log` 中 `HUD refresh suspended:` 相关行以便定位。

### 4.2 “点了没有反应，点另一个才刷新”

旧逻辑使用设置字典的字符串哈希值判断样式是否变化。哈希值不能作为内容相等的证明，存在漏判风险。

整改改为比较实际设置字典内容，并单独比较方向；同时明确设置文字的字体、字号、前景色和描边属性。系统颜色变化也会独立触发样式更新，不依赖用户再次改变设置。

这是代码层面已实施的修正。尚无完整实机回归记录证明所有触发场景都已消除。

### 4.3 文字不够粗、描边黑点及尺寸联动

用户反馈主要出现在时间模式和较大字号下。整改包括提高字重、减细描边、明确文字属性、按字重测量文字以及保留描边空间。

黑点最初没有足够的局部截图和可重复用例，不能将具体成因写成已确认结论，也不能把编译通过等同于黑点已消除。后续仍需核对具体字符、字号、背景和开关组合。

### 4.4 SpringBoard 黑屏转圈（1.0-7 已针对 `0x401` 路径加固）

早期分析基于用户提供的六份 `SpringBoard-2026-09-11-*.ips`，文件时间分别为 15:02:06、15:07:17、15:14:05、16:23:30、17:04:16、17:44:49。后续又提供五份完整日志，据此完成了确定性定位，结论见 4.1。

已确认的共同现象：

- 进程为 SpringBoard，异常为 `EXC_BAD_ACCESS / SIGBUS`。
- 故障执行地址均为 `0x401`，与项目底层 RemoteCall 返回标记对应，强烈指向该路径。
- 后五次日志属于同一系统启动会话，是 SpringBoard 反复退出重启，不能描述为每次整机重启。
- 这些日志记录的直接退出原因不是普通内存不足终止；也没有证明硬件已损坏。
- 用户确认 `1.0-1`、`1.0-2` 都出现过，且息屏时也可能发生，没有固定操作触发条件。

`1.0-9` 已针对该路径实施 4.1 所述加固，并新增 fail-closed 行为：远程调用异常时跳过刷新，而不是继续驱动可能已损坏的线程。

仍未确认或仍待验证的事项：

- `0x401` 只是故障的**表现形式**。本次修复覆盖了三条可确定的产生路径，但不排除还存在其他把裸哨兵值写入活动线程的路径；这需要实机长期观察确认。
- “崩溃是否与刷新频率相关”“降低频率是否有效”仍未验证，也不作为已完成的修复。
- 普通应用层异常捕获不能阻止另一进程中的 Mach 异常退出；本次没有改变这一限制，只是不再主动把线程置于危险状态。

发生持续黑屏转圈时，应先在应用内停用悬浮窗，再重启手机并保持停用，避免继续反复触发。不要把回退到 1.0-1 或 1.0-2 当作稳定性解决方案，因为两个版本都已有崩溃反馈。

## 5. 开发与版本维护

1. 修改前检查当前分支及工作区，避免覆盖未提交的用户改动。
2. 设置项涉及键名、读写、界面文案和实际渲染时，逐项确认都已接入。
3. 保留已有枚举值和设置含义；新增值尽量追加，不调整历史值顺序。
4. 涉及字号、字重和描边时，同步检查测量尺寸与绘制属性。
5. 涉及刷新间隔时，同步检查采样时间、FPS、方向检测、颜色检测及事件即时更新。
6. 新增文案维护中文、英文、西班牙文资源。
7. 发出新测试包前更新工程中的构建号。当前工程有六处 `CURRENT_PROJECT_VERSION`，需要保持一致。
8. 检查差异格式并完成云端构建；如无实机验证，应明确标记为待验证。

Windows 开发环境使用 PowerShell 7。Windows 可以编辑源码、检查 Git 差异及校验安装包，但当前环境没有 Xcode，不能在本机完成 iOS 构建。

## 6. 编译流程

### 6.1 macOS 本地构建

当前已通过测试工作流验证的工具组合为 macOS 15、Xcode 16.4。开发者需要完整源码及项目依赖，在仓库根目录执行：

```sh
# 查看生成的版本标识，例如 1.0-6
./get-version.sh

# 生成未签名 IPA，供后续签名安装
DARKSPEED_UNSIGNED=1 ./build-darkspeed.sh
```

已有有效 Xcode 签名配置时，脚本也提供签名构建入口：

```sh
./build-darkspeed.sh
```

本次实际交付均使用未签名构建路径；未在当前会话中验证开发者本机的签名配置。

脚本执行 `clean build archive`，使用 `DarkSpeed` scheme、Release 配置和真机 iOS SDK。脚本指定部署目标 iOS 16.0，但部署目标不能代表所有更高版本都实际兼容。

输出：

```text
DarkSpeed.xcarchive/
packages/DarkSpeed_1.0-6.ipa
```

实际文件名来自工程配置，不是写死的 `1.0-6`。可通过 `DARKSPEED_ARCHIVE_PATH` 更改归档路径。

### 6.2 GitHub Actions 测试构建

当前测试工作流：`.github/workflows/build-test-ipa.yml`，名称 **Build Test IPA**。

1. 将代码提交到开发仓库的 `codex/clock-top-positions` 分支；推送会自动触发测试构建。
2. 打开仓库 Actions，查看 **Build Test IPA**。
3. 确认 Checkout、Set up Xcode、Build IPA、Upload IPA 均成功。
4. 下载运行页面的 `DarkSpeed-test-运行序号` 附件。
5. 解压附件 ZIP，取出其中的 `.ipa`，不要把附件 ZIP 当成 IPA 安装。

工作流也声明了 `workflow_dispatch`。GitHub 是否展示手动运行入口取决于工作流所在分支及平台条件；没有入口时不要据此判断自动构建无法运行。当前已验证的是指定分支推送触发流程。

| 配置 | 当前值 |
|---|---|
| 运行环境 | `macos-15` |
| Xcode | `16.4` |
| 超时 | 30 分钟 |
| 构建模式 | 未签名 IPA |
| 工作流权限 | `contents: read` |
| 附件保留 | 14 天 |

附件序号是工作流运行序号，不等于应用构建号。虽然目前 1～6 恰好对应，重跑或额外触发后不一定相同，应查看 IPA 内的版本信息。

最新已验证构建：<https://github.com/wozuishuai997/DarkSpeed/actions/runs/34659014359>。

### 6.3 测试构建与公开发布的区别

`build-test-ipa.yml` 只构建并上传 Actions 附件，不创建 Release。但开发仓库目前是公开仓库，不能把“未创建 Release”理解成“源码和附件私密”。

`build-release.yml` 会上传 GitHub Release，并具有 `contents: write` 权限；其现有配置是 macOS 14 / Xcode 15.4，与本次测试流程不同。该流程不属于本次已验证的交付路径。仅需测试 IPA 时不要误触发 Release 流程或发布标签。

## 7. 签名、安装与升级

1. 保存当前安装包和设置记录，便于比较显示效果。
2. 在旧版 DarkSpeed 内关闭悬浮窗，再进行升级。
3. 将目标 IPA 传到手机，使用 LCSign 和有效的证书、描述文件签名。
4. 安装签名后的应用。能否覆盖安装取决于应用标识及签名配置；卸载重装可能丢失设置。
5. 首次运行允许应用联网，再启用悬浮窗。
6. 核对应用版本与新增设置，按下节进行实机验证。

用户此前使用 LCSign，沿用同一证书，曾卸载原版后重新安装。早期一次初始化失败在重启后恢复，这只能记录为一次恢复结果，不能视为所有初始化问题或 SpringBoard 崩溃的通用修复。

原项目提示：悬浮窗启用期间不要从多任务界面强制结束应用。需要停用时，先使用应用内关闭悬浮窗功能。

## 8. 验证与验收清单

### 8.1 已完成的验证

- 1.0-1～1.0-9 已生成测试 IPA；历次云端构建记录显示编译归档成功。
- 1.0-9 的云端构建 `Build Test IPA` 运行 #10（run `34662549118`，提交 `d0aff7b`）四步全部成功：Checkout / Set up Xcode / Build IPA / Upload IPA。（1.0-8 为运行 #9，run `34662277913`，提交 `00fbd4d`；1.0-7 为运行 #8，run `34662060600`，提交 `421cb77`，同样全部成功。）
- 1.0-9 修改的源文件未产生新的编译警告；这不表示仓库所有第三方源码没有警告（`sources/SPLarkController/*` 仍有既有弃用警告）。
- 已检查 1.0-9 附件的 SHA-256、应用标识、版本号、构建号，并确认八处修复字符串编译进主二进制、且已撤掉的内核回读校验字符串确实不存在。
- 已确认主二进制为 arm64e（`cpusubtype 0x0100000c`），与 PAC 路径和 iPhone 15 Pro 匹配。
- 当前代码、构建配置和本地安装包版本已核对。

### 8.2 待完成的实机验收

| 场景 | 应观察的结果 |
|---|---|
| **开启悬浮窗（1.0-6 的必崩场景）** | **本次首要验收项**：开关悬浮窗多次，SpringBoard 不应再出现黑屏转圈 |
| 四种显示模式循环 | 模式正确，时间与系统一致 |
| 12/24 小时制与时区变化 | 下一次刷新后使用系统格式 |
| 字号 8、16、24 | 无裁切、异常换行或明显描边黑点 |
| 加粗、背景、字号单独切换 | 不需要点击其他选项才能生效 |
| 水平偏移 −100、0、+100 | 方向正确，左中右及顶部都生效；较大偏移可能把文字移到屏幕边缘之外 |
| 透明背景：白底应用、深色界面、锁屏 | 到下一次刷新时，文字与右侧状态栏黑白样式一致，描边相反 |
| 透明切回原样 | 恢复背景和手动外观，清除透明模式描边 |
| 间隔 1、5、30 秒 | 自动刷新节奏改变，网速/FPS 不按累计时长成倍放大 |
| 修改设置、锁屏、解锁 | 可以额外即时更新，不应误判为间隔设置无效 |
| 视频进入/退出横屏 | 按当前周期检测方向并执行隐藏/显示；不按即时刷新验收 |
| 连续使用及息屏 | 记录是否再次发生黑屏转圈；未完成前不能宣称崩溃已解决 |
| 出现「为保护 SpringBoard，已停止 HUD 刷新」 | 属预期 fail-closed 行为。记录该文案及 `DSBridge.log` 中 `HUD refresh suspended:` 行，用于定位是哪个远程调用异常 |

排查显示问题时记录：安装版本、显示模式、字号、加粗、背景、偏移、刷新间隔、发生场景及截图。排查崩溃时保留原始 `.ips` 和发生时间。提交外部报告前检查日志中的设备标识等隐私信息。

## 9. 当前安装包校验值

`packages/DarkSpeed_1.0-9.ipa`，968784 字节：

```text
SHA256
a10e9aacd0e59d9c71e0ebdf99231e4cf5ce3fd9298e7791025aed839b4f03a9
```

对应 Actions 附件 `DarkSpeed-test-10.zip`（963841 字节）：

```text
SHA256
3fd4a152a7aff74040ffc810e493b43892e0f2a9adf335959fd0d279612248ff
```

包内 `Info.plist` 已核验：`CFBundleIdentifier = com.huami.darkspeed`、`CFBundleVersion = 9`、`CFBundleShortVersionString = 1.0`。

早期版本不要用于验证本问题：1.0-7 会在 PAC 签名失败时把裸标记回复给内核（主线程 PAC_EXCEPTION），1.0-8 缺少创建阶段的闸门。请直接使用 1.0-9。

Windows 校验示例，在仓库根目录的 PowerShell 7 中执行：

```powershell
Get-FileHash -Algorithm SHA256 -LiteralPath './packages/DarkSpeed_1.0-9.ipa'
```

macOS 校验示例：

```sh
shasum -a 256 packages/DarkSpeed_1.0-9.ipa
```

LCSign 重新签名会改变 IPA 内容，签名后的哈希不同属于正常现象；上述值用于验证签名前的本次交付文件。重新构建相同源码也不保证得到逐字节相同的安装包。

## 10. 后续维护重点

1. **优先验证“开启悬浮窗”这一必崩场景**：1.0-6 下开启悬浮窗即导致 SpringBoard 崩溃；1.0-9 已修掉直接原因（内核拒绝未签名的标记 PC），应已消除。请在 iOS 18.6.2 上反复开关悬浮窗并保留新的 `.ips`（如有）。该加固只在代码层面完成，尚缺实机证据。
2. 若再次出现黑屏转圈，先比对故障地址：仍是 `0x401`（或 `0x101/0x201/0x301`）说明还有未覆盖的裸哨兵值写入路径；换到其他地址则属于新的问题类别。
3. 优先补齐 iOS 18.6.2 上的显示及颜色跟随实测记录。
4. 记录 fail-closed 是否被触发：出现「为保护 SpringBoard，已停止 HUD 刷新」时，收集 `DSBridge.log` 中 `HUD refresh suspended:` 行，定位具体失败的远程调用。
5. 明确记录刷新间隔变化与黑屏转圈之间的观察结果，不能仅凭减少次数就断言根因已修复。
6. 横竖屏独立即时刷新仍是待决项，不要在发布说明中写为已完成。
7. 后续正式交付前同步更新 README 版本与验证边界，并保存新包校验值。
8. 每次更新本说明时写明源码提交、构建号、实际构建记录和实机验证状态。

本说明是开发交接文档。`1.0-9` 的代码修复与云端构建已完成，实机验证仍待用户执行。

> 推送提示：本机到 `github.com` 的 git 协议会被 DNS 污染与连接重置阻断，需为 git 指定可用代理（本机验证可用 `http://127.0.0.1:10809`）才能推送并触发 `Build Test IPA`。
